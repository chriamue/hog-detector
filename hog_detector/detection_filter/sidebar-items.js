window.SIDEBAR_ITEMS = {"struct":[["TrackerFilter","`TrackerFilter` is a struct for tracking and filtering object detections over time. It refines the detections by comparing the overlap between the previous frame’s detections and the current frame’s detections, using an overlap threshold to filter out false positives."]],"trait":[["DetectionFilter","Defines a trait for filtering detections to eliminate false positive detections."]]};